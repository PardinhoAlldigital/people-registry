services:
  postgres:
    image: postgres:16-alpine
    container_name: people_db
    restart: always
    environment:
      POSTGRES_DB: people_registry
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d people_registry"]
      interval: 5s
      timeout: 5s
      retries: 10

  api:
    build:
      context: ./api
      dockerfile: Dockerfile
      target: production
    container_name: people_api
    restart: always
    ports:
      - "4000:4000"
    environment:
      DATABASE_URL: postgresql://postgres:${POSTGRES_PASSWORD:-postgres}@postgres:5432/people_registry
      JWT_SECRET: ${JWT_SECRET:-super-secret-jwt-key-change-in-production-32chars}
      PORT: 4000
      CORS_ORIGIN: ${APP_URL:-http://localhost:3000}
      NODE_ENV: production
    depends_on:
      postgres:
        condition: service_healthy
    command: sh -c "npx prisma migrate deploy && npm start"

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: people_app
    restart: always
    ports:
      - "3000:3000"
    environment:
      NEXT_PUBLIC_API_URL: ${API_URL:-http://localhost:4000}
      NODE_ENV: production
    depends_on:
      - api

volumes:
  postgres_data:
    name: people_registry_postgres_data
